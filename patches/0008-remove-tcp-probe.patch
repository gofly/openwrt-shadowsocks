From 257663d9f04045c31eddebc24d4efffc17d88f97 Mon Sep 17 00:00:00 2001
From: zhenglixin <lixin.zhenglx@gmail.com>
Date: Wed, 24 Sep 2025 13:03:59 +0800
Subject: [PATCH 8/9] remove tcp probe

---
 src/CMakeLists.txt |   1 +
 src/Makefile.am    |   3 +-
 src/common.h       |   3 +-
 src/netutils.c     |  31 +++++++++
 src/netutils.h     |   2 +
 src/probe.c        | 158 ++++++++++++---------------------------------
 src/probe.h        |   7 +-
 src/redir.c        | 137 ++++++++++++++++-----------------------
 src/redir.h        |  12 ++--
 src/udprelay.c     |  66 ++++---------------
 src/udprelay.h     |   4 +-
 src/utils.c        |  10 +++
 src/utils.h        |   1 +
 13 files changed, 166 insertions(+), 269 deletions(-)

diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 65f44fb..eb76252 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -25,6 +25,7 @@ set(SS_SHARED_SOURCES
 set(SS_REDIR_SOURCE
         ${SS_SHARED_SOURCES}
         udprelay.c
+        probe.c
         cache.c
         redir.c
         ${SS_CRYPTO_SOURCE}
diff --git a/src/Makefile.am b/src/Makefile.am
index 5a977fd..7f97ee9 100644
--- a/src/Makefile.am
+++ b/src/Makefile.am
@@ -34,6 +34,7 @@ ss_redir_SOURCES = utils.c \
                    netutils.c \
                    cache.c \
                    udprelay.c \
+                   probe.c \
                    redir.c \
                    $(crypto_src) \
                    $(plugin_src)
@@ -44,4 +45,4 @@ ss_redir_LDADD += -lcares
 
 noinst_HEADERS = crypto.h stream.h aead.h json.h netutils.h redir.h uthash.h \
                  cache.h plugin.h utils.h base64.h ppbloom.h \
-                 common.h jconf.h udprelay.h
+                 common.h jconf.h udprelay.h probe.h
diff --git a/src/common.h b/src/common.h
index 95c0705..2926713 100644
--- a/src/common.h
+++ b/src/common.h
@@ -30,7 +30,8 @@
 
 int init_udprelay(const char *server_host, const char *server_port,
                   int remote_num, struct sockaddr **remote_addr, int mtu,
-                  crypto_t *crypto, int timeout, const char *iface, int fwmark);
+                  crypto_t *crypto, int timeout, const char *iface, int fwmark,
+                  volatile bool *remote_status);
 
 void free_udprelay(void);
 
diff --git a/src/netutils.c b/src/netutils.c
index 7b32805..64a9ddd 100644
--- a/src/netutils.c
+++ b/src/netutils.c
@@ -123,6 +123,37 @@ bind_to_addr(struct sockaddr_storage *storage,
     return -1;
 }
 
+int
+construct_udprelay_header(const struct sockaddr_storage *in_addr,
+                          char *addr_header)
+{
+    int addr_header_len = 0;
+
+    if (in_addr->ss_family == AF_INET) {
+        struct sockaddr_in *addr = (struct sockaddr_in *)in_addr;
+        size_t addr_len          = sizeof(struct in_addr);
+
+        addr_header[addr_header_len++] = 1;
+        memcpy(addr_header + addr_header_len, &addr->sin_addr, addr_len);
+        addr_header_len += addr_len;
+        memcpy(addr_header + addr_header_len, &addr->sin_port, 2);
+        addr_header_len += 2;
+    } else if (in_addr->ss_family == AF_INET6) {
+        struct sockaddr_in6 *addr = (struct sockaddr_in6 *)in_addr;
+        size_t addr_len           = sizeof(struct in6_addr);
+
+        addr_header[addr_header_len++] = 4;
+        memcpy(addr_header + addr_header_len, &addr->sin6_addr, addr_len);
+        addr_header_len += addr_len;
+        memcpy(addr_header + addr_header_len, &addr->sin6_port, 2);
+        addr_header_len += 2;
+    } else {
+        return 0;
+    }
+
+    return addr_header_len;
+}
+
 ssize_t
 get_sockaddr(char *host, char *port,
              struct sockaddr_storage *storage, int block,
diff --git a/src/netutils.h b/src/netutils.h
index 8272419..6aeaf3c 100644
--- a/src/netutils.h
+++ b/src/netutils.h
@@ -81,6 +81,8 @@ ssize_t get_sockaddr(char *host, char *port,
                      struct sockaddr_storage *storage, int block,
                      int ipv6first);
 int set_reuseport(int socket);
+int construct_udprelay_header(const struct sockaddr_storage *in_addr,
+                              char *addr_header);
 
 #ifdef SET_INTERFACE
 int setinterface(int socket_fd, const char *interface_name);
diff --git a/src/probe.c b/src/probe.c
index 36c2e94..c3f468a 100644
--- a/src/probe.c
+++ b/src/probe.c
@@ -31,21 +31,10 @@
 #include "probe.h"
 #include "utils.h"
 #include "netutils.h"
+#include "udprelay.h"
 
 #define MAX_LISTENERS 32
 
-/* TCP Probe */
-typedef struct tcp_probe_ctx {
-    ev_io io;
-    ev_timer watcher;
-    int remote_idx;
-    listen_ctx_t *listener;
-} tcp_probe_ctx_t;
-
-static ev_timer tcp_probe_timer;
-static listen_ctx_t *tcp_listeners[MAX_LISTENERS] = { NULL };
-static int tcp_listener_count = 0;
-
 /* UDP Probe */
 typedef struct udp_probe_ctx {
     ev_io io;
@@ -59,93 +48,17 @@ static ev_timer udp_probe_timer;
 static server_ctx_t *udp_servers[MAX_LISTENERS] = { NULL };
 static int udp_server_count = 0;
 
-static const unsigned char dns_probe_packet[] = {
-    0xDB, 0x42, 0x01, 0x00, 0x00, 0x01, 0x00, 0x00,
-    0x00, 0x00, 0x00, 0x00, 0x03, 0x77, 0x77, 0x77,
-    0x06, 0x67, 0x6f, 0x6f, 0x67, 0x6c, 0x65, 0x03,
-    0x63, 0x6f, 0x6d, 0x00, 0x00, 0x01, 0x00, 0x01
+/* DNS query payload for www.google.com (A record) */
+/* This is just the question section of a DNS query */
+static const unsigned char dns_probe_payload[] = {
+    /* QNAME: 3www6google3com0 */
+    0x03, 0x77, 0x77, 0x77, 0x06, 0x67, 0x6f, 0x6f, 0x67, 0x6c, 0x65, 0x03, 0x63, 0x6f, 0x6d, 0x00,
+    /* QTYPE: A */
+    0x00, 0x01,
+    /* QCLASS: IN */
+    0x00, 0x01
 };
 
-/* TCP Probe Implementation */
-static void tcp_probe_cleanup(EV_P_ tcp_probe_ctx_t *p_ctx) {
-    ev_io_stop(EV_A_ &p_ctx->io);
-    ev_timer_stop(EV_A_ &p_ctx->watcher);
-    close(p_ctx->io.fd);
-    ss_free(p_ctx);
-}
-
-static void tcp_probe_connect_cb(EV_P_ ev_io *w, int revents) {
-    tcp_probe_ctx_t *p_ctx = (tcp_probe_ctx_t *)w;
-    int err;
-    socklen_t len = sizeof(err);
-
-    if (getsockopt(w->fd, SOL_SOCKET, SO_ERROR, &err, &len) == -1) {
-        err = errno;
-    }
-
-    if (err == 0) {
-        if (!p_ctx->listener->remote_status[p_ctx->remote_idx]) {
-            LOGI("[tcp-probe] remote %d is back online.", p_ctx->remote_idx);
-        }
-        p_ctx->listener->remote_status[p_ctx->remote_idx] = true;
-    } else {
-        if (p_ctx->listener->remote_status[p_ctx->remote_idx]) {
-            LOGI("[tcp-probe] remote %d is offline: %s", p_ctx->remote_idx, strerror(err));
-        }
-        p_ctx->listener->remote_status[p_ctx->remote_idx] = false;
-    }
-    tcp_probe_cleanup(EV_A_ p_ctx);
-}
-
-static void tcp_probe_timeout_cb(EV_P_ ev_timer *w, int revents) {
-    tcp_probe_ctx_t *p_ctx = cork_container_of(w, tcp_probe_ctx_t, watcher);
-    if (p_ctx->listener->remote_status[p_ctx->remote_idx]) {
-        LOGI("[tcp-probe] remote %d timed out.", p_ctx->remote_idx);
-    }
-    p_ctx->listener->remote_status[p_ctx->remote_idx] = false;
-    tcp_probe_cleanup(EV_A_ p_ctx);
-}
-
-static void start_one_tcp_probe(EV_P_ listen_ctx_t *listener, int idx) {
-    struct sockaddr *remote_addr = listener->remote_addr[idx];
-    int probefd = socket(remote_addr->sa_family, SOCK_STREAM, 0);
-    if (probefd == -1) {
-        listener->remote_status[idx] = false;
-        return;
-    }
-
-    setnonblocking(probefd);
-
-    tcp_probe_ctx_t *p_ctx = ss_malloc(sizeof(tcp_probe_ctx_t));
-    memset(p_ctx, 0, sizeof(tcp_probe_ctx_t));
-    p_ctx->listener = listener;
-    p_ctx->remote_idx = idx;
-
-    ev_io_init(&p_ctx->io, tcp_probe_connect_cb, probefd, EV_WRITE);
-    ev_timer_init(&p_ctx->watcher, tcp_probe_timeout_cb, min(MAX_CONNECT_TIMEOUT, 5), 0);
-
-    if (connect(probefd, remote_addr, get_sockaddr_len(remote_addr)) != 0 && errno != EINPROGRESS) {
-        listener->remote_status[idx] = false;
-        close(probefd);
-        ss_free(p_ctx);
-        return;
-    }
-
-    ev_io_start(EV_A_ &p_ctx->io);
-    ev_timer_start(EV_A_ &p_ctx->watcher);
-}
-
-static void tcp_probe_timer_cb(EV_P_ ev_timer *w, int revents) {
-    for (int i = 0; i < tcp_listener_count; i++) {
-        listen_ctx_t *lc = tcp_listeners[i];
-        if (lc && lc->remote_num > 1) {
-            for (int j = 0; j < lc->remote_num; j++) {
-                start_one_tcp_probe(EV_A_ lc, j);
-            }
-        }
-    }
-}
-
 /* UDP Probe Implementation */
 static void udp_probe_cleanup(EV_P_ udp_probe_ctx_t *p_ctx) {
     ev_io_stop(EV_A_ &p_ctx->io);
@@ -164,7 +77,15 @@ static void udp_probe_recv_cb(EV_P_ ev_io *w, int revents) {
     if (r > 0) {
         buf->len = r;
         if (p_ctx->crypto->decrypt_all(buf, p_ctx->crypto->cipher, buf->capacity) == 0) {
-            if (buf->len > 3 && (buf->data[3] & 0x0F) == 0) {
+            /*
+             * A successful probe requires:
+             * 1. DNS RCODE is 0 (No Error).
+             * 2. ANCOUNT (Answer Record Count) is greater than 0.
+             */
+            #define DNS_RCODE_OK(buf) ((buf->len >= 12) && ((buf->data[3] & 0x0F) == 0))
+            #define DNS_HAS_ANSWERS(buf) (load16_be(buf->data + 6) > 0)
+
+            if (DNS_RCODE_OK(buf) && DNS_HAS_ANSWERS(buf)){
                 success = true;
             }
         }
@@ -203,10 +124,25 @@ static void start_one_udp_probe(EV_P_ server_ctx_t *s_ctx, int idx) {
 
     setnonblocking(probefd);
 
+    /* Construct shadowsocks UDP packet: [addr header][dns query] */
+    struct sockaddr_storage dns_server_addr;
+    if (get_sockaddr("8.8.8.8", "53", &dns_server_addr, 1, 0) != 0) {
+        LOGE("[udp-probe] failed to resolve 8.8.8.8");
+        close(probefd);
+        return;
+    }
+
+    char addr_header[MAX_ADDR_HEADER_SIZE] = {0};
+    int addr_header_len = construct_udprelay_header(&dns_server_addr, addr_header);
+
     buffer_t *buf = ss_malloc(sizeof(buffer_t));
-    balloc(buf, sizeof(dns_probe_packet) + MAX_ADDR_HEADER_SIZE);
-    memcpy(buf->data, dns_probe_packet, sizeof(dns_probe_packet));
-    buf->len = sizeof(dns_probe_packet);
+    balloc(buf, addr_header_len + sizeof(dns_probe_payload));
+
+    /* Prepend addr header */
+    memcpy(buf->data, addr_header, addr_header_len);
+    /* Append DNS query payload */
+    memcpy(buf->data + addr_header_len, dns_probe_payload, sizeof(dns_probe_payload));
+    buf->len = addr_header_len + sizeof(dns_probe_payload);
 
     if (s_ctx->crypto->encrypt_all(buf, s_ctx->crypto->cipher, buf->capacity) != 0) {
         bfree(buf);
@@ -243,23 +179,12 @@ static void udp_probe_timer_cb(EV_P_ ev_timer *w, int revents) {
 }
 
 /* Public Interface */
-void probe_init(EV_P_ int tcp_interval, int udp_interval) {
-    ev_timer_init(&tcp_probe_timer, tcp_probe_timer_cb, tcp_interval, tcp_interval);
+void probe_init(EV_P_ int udp_interval) {
     ev_timer_init(&udp_probe_timer, udp_probe_timer_cb, udp_interval, udp_interval);
 }
 
-void probe_add_tcp_listener(listen_ctx_t *listener) {
-    if (tcp_listener_count < MAX_LISTENERS) {
-        tcp_listeners[tcp_listener_count++] = listener;
-        if (listener->remote_num > 1 && !ev_is_active(&tcp_probe_timer)) {
-            ev_timer_start(EV_DEFAULT, &tcp_probe_timer);
-        }
-    } else {
-        LOGE("[probe] too many tcp listeners");
-    }
-}
-
-void probe_add_udp_server(server_ctx_t *server) {
+void probe_add_udp_server(void *server_ptr) {
+    server_ctx_t *server = (server_ctx_t *)server_ptr;
     if (udp_server_count < MAX_LISTENERS) {
         udp_servers[udp_server_count++] = server;
         if (server->remote_num > 1 && !ev_is_active(&udp_probe_timer)) {
@@ -271,9 +196,6 @@ void probe_add_udp_server(server_ctx_t *server) {
 }
 
 void probe_cleanup(EV_P) {
-    if (ev_is_active(&tcp_probe_timer)) {
-        ev_timer_stop(EV_A_ &tcp_probe_timer);
-    }
     if (ev_is_active(&udp_probe_timer)) {
         ev_timer_stop(EV_A_ &udp_probe_timer);
     }
diff --git a/src/probe.h b/src/probe.h
index e692aa5..3b7f86c 100644
--- a/src/probe.h
+++ b/src/probe.h
@@ -24,12 +24,9 @@
 #define _PROBE_H
 
 #include <ev.h>
-#include "redir.h"
-#include "udprelay.h"
 
-void probe_init(EV_P_ int tcp_interval, int udp_interval);
-void probe_add_tcp_listener(listen_ctx_t *listener);
-void probe_add_udp_server(server_ctx_t *server);
+void probe_init(EV_P_ int udp_interval);
+void probe_add_udp_server(void *server);
 void probe_cleanup(EV_P);
 
 #endif // _PROBE_H
\ No newline at end of file
diff --git a/src/redir.c b/src/redir.c
index ceb563f..2e54146 100644
--- a/src/redir.c
+++ b/src/redir.c
@@ -52,6 +52,7 @@
 #include "utils.h"
 #include "common.h"
 #include "redir.h"
+#include "probe.h"
 
 #ifndef EAGAIN
 #define EAGAIN EWOULDBLOCK
@@ -111,7 +112,7 @@ static struct ev_signal sigchld_watcher;
 
 static int tcp_tproxy = 0; /* use tproxy instead of redirect (for tcp) */
 
-#define MAX_LISTEN_CTX 32
+#define MAX_LISTEN_CTX 128
 static listen_ctx_t *listen_ctx_list[MAX_LISTEN_CTX] = { NULL };
 static int listen_ctx_count                          = 0;
 
@@ -136,16 +137,6 @@ getdestaddr(int fd, struct sockaddr_storage *destaddr)
     return 0;
 }
 
-int
-setnonblocking(int fd)
-{
-    int flags;
-    if (-1 == (flags = fcntl(fd, F_GETFL, 0))) {
-        flags = 0;
-    }
-    return fcntl(fd, F_SETFL, flags | O_NONBLOCK);
-}
-
 int
 create_and_bind(const char *addr, const char *port)
 {
@@ -226,7 +217,7 @@ static void
 server_recv_cb(EV_P_ ev_io *w, int revents)
 {
     server_ctx_t *server_recv_ctx = (server_ctx_t *)w;
-    server_t *server              = server_recv_ctx->server;
+    server_t *server             = server_recv_ctx->server;
     remote_t *remote              = server->remote;
 
     ev_timer_stop(EV_A_ & server->delayed_connect_watcher);
@@ -318,7 +309,7 @@ static void
 server_send_cb(EV_P_ ev_io *w, int revents)
 {
     server_ctx_t *server_send_ctx = (server_ctx_t *)w;
-    server_t *server              = server_send_ctx->server;
+    server_t *server             = server_send_ctx->server;
     remote_t *remote              = server->remote;
     if (server->buf->len == 0) {
         // close and free
@@ -398,7 +389,7 @@ static void
 remote_recv_cb(EV_P_ ev_io *w, int revents)
 {
     remote_ctx_t *remote_recv_ctx = (remote_ctx_t *)w;
-    remote_t *remote              = remote_recv_ctx->remote;
+    remote_t *remote                    = remote_recv_ctx->remote;
     server_t *server              = remote->server;
 
     ssize_t r = recv(remote->fd, server->buf->data, SOCKET_BUF_SIZE, 0);
@@ -467,8 +458,8 @@ static void
 remote_send_cb(EV_P_ ev_io *w, int revents)
 {
     remote_ctx_t *remote_send_ctx = (remote_ctx_t *)w;
-    remote_t *remote              = remote_send_ctx->remote;
-    server_t *server              = remote->server;
+    remote_t *remote                    = remote_send_ctx->remote;
+    server_t *server                    = remote->server;
 
     ev_timer_stop(EV_A_ & remote_send_ctx->watcher);
 
@@ -795,7 +786,7 @@ start_connect_remote(EV_P_ server_t *server)
 
     int remotefd = socket(remote_addr->sa_family, SOCK_STREAM, IPPROTO_TCP);
     if (remotefd == -1) {
-        ERROR("socket");
+        ERROR("[tcp] socket");
         failover_to_next_remote(EV_A_ server);
         return;
     }
@@ -835,7 +826,7 @@ start_connect_remote(EV_P_ server_t *server)
     if (listener->mptcp > 1) {
         int err = setsockopt(remotefd, SOL_TCP, listener->mptcp, &opt, sizeof(opt));
         if (err == -1) {
-            ERROR("failed to enable multipath TCP");
+            ERROR("[tcp] failed to enable multipath TCP");
         }
     } else if (listener->mptcp == 1) {
         int i = 0;
@@ -847,7 +838,7 @@ start_connect_remote(EV_P_ server_t *server)
             i++;
         }
         if (listener->mptcp == 0) {
-            ERROR("failed to enable multipath TCP");
+            LOGE("[tcp] failed to enable multipath TCP");
         }
     }
 
@@ -871,7 +862,7 @@ start_connect_remote(EV_P_ server_t *server)
         int r = connect(remotefd, remote_addr, get_sockaddr_len(remote_addr));
 
         if (r == -1 && errno != CONNECT_IN_PROGRESS) {
-            ERROR("connect");
+            ERROR("[tcp] connect");
             failover_to_next_remote(EV_A_ server);
             return;
         }
@@ -893,13 +884,13 @@ accept_cb(EV_P_ ev_io *w, int revents)
 
     int serverfd = accept(listener->fd, NULL, NULL);
     if (serverfd == -1) {
-        ERROR("accept");
+        ERROR("[tcp] accept");
         return;
     }
 
     err = getdestaddr(serverfd, &destaddr);
     if (err) {
-        ERROR("getdestaddr");
+        ERROR("[tcp] getdestaddr");
         return;
     }
 
@@ -946,14 +937,15 @@ signal_cb(EV_P_ ev_signal *w, int revents)
             } else
                 return;
         case SIGINT:
-        case SIGTERM:
+        case SIGTERM: {
             ev_signal_stop(EV_DEFAULT, &sigint_watcher);
             ev_signal_stop(EV_DEFAULT, &sigterm_watcher);
             ev_signal_stop(EV_DEFAULT, &sigchld_watcher);
-    probe_cleanup(EV_A);
+            probe_cleanup(EV_A);
 
             ev_unloop(EV_A_ EVUNLOOP_ALL);
         }
+        }
     }
 }
 
@@ -1299,7 +1291,9 @@ main(int argc, char **argv)
     ev_signal_start(EV_DEFAULT, &sigterm_watcher);
     ev_signal_start(EV_DEFAULT, &sigchld_watcher);
 
-    probe_init(EV_A_ 60, 60);
+    struct ev_loop *loop = EV_DEFAULT;
+
+    probe_init(EV_A_ 60);
     LOGI("initializing ciphers... %s", method);
     crypto = crypto_init(password, key, method);
     if (crypto == NULL)
@@ -1335,78 +1329,57 @@ main(int argc, char **argv)
     listen_ctx.timeout = atoi(timeout);
     listen_ctx.mptcp   = mptcp;
 
-    struct ev_loop *loop = EV_DEFAULT;
-
-    /* we will possibly create multiple listen_ctx instances (for DSCP),
-     * so iterate and keep pointers in listen_ctx_list.
+    /*
+     * Initialize UDP relay first if enabled.
+     * This is crucial because the UDP module allocates and manages the
+     * `remote_status` array that the TCP module will share.
      */
-    listen_ctx_t *listen_ctx_current = &listen_ctx;
-
-    /* register first (stack) listen_ctx */
-    if (listen_ctx_count < MAX_LISTEN_CTX) {
-        listen_ctx_list[listen_ctx_count++] = listen_ctx_current;
-        probe_add_tcp_listener(listen_ctx_current);
-    } else {
-        LOGE("too many listen ctx; increase MAX_LISTEN_CTX");
+    if (mode != TCP_ONLY) {
+        init_udprelay(local_addr, local_port, listen_ctx.remote_num,
+                      listen_ctx.remote_addr, mtu, crypto,
+                      listen_ctx.timeout, NULL, fwmark, listen_ctx.remote_status);
     }
 
-    do {
-        if (listen_ctx_current->tos) {
-            LOGI("listening at %s:%s (TOS 0x%x)", local_addr, local_port, listen_ctx_current->tos);
-        } else {
-            LOGI("listening at %s:%s", local_addr, local_port);
-        }
-
-        if (mode != UDP_ONLY) {
-            /* Setup socket */
-            int listenfd;
-            listenfd = create_and_bind(local_addr, local_port);
-            if (listenfd == -1) {
-                FATAL("bind() error");
-            }
-            if (listen(listenfd, SOMAXCONN) == -1) {
-                FATAL("listen() error");
+    /*
+     * Now, set up TCP listeners. They will all share the same `remote_status`
+     * array, which is either managed by the UDP prober or statically set to true.
+     */
+    if (mode != UDP_ONLY) {
+        listen_ctx_t *listen_ctx_current = &listen_ctx;
+        do {
+            if (listen_ctx_current->tos) {
+                LOGI("listening at %s:%s (TOS 0x%x)", local_addr, local_port, listen_ctx_current->tos);
+            } else {
+                LOGI("listening at %s:%s", local_addr, local_port);
             }
+
+            int listenfd = create_and_bind(local_addr, local_port);
+            if (listenfd == -1) FATAL("bind() error");
+            if (listen(listenfd, SOMAXCONN) == -1) FATAL("listen() error");
             setnonblocking(listenfd);
 
             listen_ctx_current->fd = listenfd;
-
             ev_io_init(&listen_ctx_current->io, accept_cb, listenfd, EV_READ);
             ev_io_start(loop, &listen_ctx_current->io);
-        }
-
-        /* Setup UDP */
-        if (mode != TCP_ONLY) {
-            LOGI("UDP relay enabled");
-            init_udprelay(local_addr, local_port, listen_ctx_current->remote_num,
-                          listen_ctx_current->remote_addr, mtu, crypto,
-                          listen_ctx_current->timeout, NULL, fwmark);
-        }
 
-        if (mode == UDP_ONLY) {
-            LOGI("TCP relay disabled");
-        }
-
-        /* Handle additional TOS/DSCP listening ports (if any) */
-        if (dscp_num > 0) {
-            /* allocate new listen_ctx, copy from listen_ctx (stack) and update values */
-            listen_ctx_t *new_lc = (listen_ctx_t *)ss_malloc(sizeof(listen_ctx_t));
-            memcpy(new_lc, &listen_ctx, sizeof(listen_ctx_t));
-            /* set the port and tos for this new listener */
-            local_port = dscp[dscp_num - 1].port;
-            new_lc->tos = dscp[dscp_num - 1].dscp << 2;
-
-            listen_ctx_current = new_lc;
-
-            /* register the newly allocated listener pointer for failback */
             if (listen_ctx_count < MAX_LISTEN_CTX) {
                 listen_ctx_list[listen_ctx_count++] = listen_ctx_current;
-                probe_add_tcp_listener(listen_ctx_current);
             } else {
                 LOGE("too many listen ctx; increase MAX_LISTEN_CTX");
             }
-        }
-    } while (dscp_num-- > 0);
+
+            /* Handle additional TOS/DSCP listening ports */
+            if (dscp_num > 0) {
+                listen_ctx_t *new_lc = (listen_ctx_t *)ss_malloc(sizeof(listen_ctx_t));
+                memcpy(new_lc, &listen_ctx, sizeof(listen_ctx_t));
+                local_port = dscp[dscp_num - 1].port;
+                new_lc->tos = dscp[dscp_num - 1].dscp << 2;
+                listen_ctx_current = new_lc;
+            }
+        } while (dscp_num-- > 0);
+    } else {
+        LOGI("TCP relay disabled");
+    }
 
     /* setuid */
     if (user != NULL && !run_as(user)) {
diff --git a/src/redir.h b/src/redir.h
index 5472dea..3c134b6 100644
--- a/src/redir.h
+++ b/src/redir.h
@@ -53,10 +53,10 @@ typedef struct server {
 
     buffer_t *buf;
 
-    cipher_ctx_t *e_ctx;
-    cipher_ctx_t *d_ctx;
-    struct server_ctx *recv_ctx;
-    struct server_ctx *send_ctx;
+    cipher_ctx_t *e_ctx; // encryption context
+    cipher_ctx_t *d_ctx; // decryption context
+    server_ctx_t *recv_ctx;
+    server_ctx_t *send_ctx;
     struct remote *remote;
 
     struct sockaddr_storage destaddr;
@@ -75,8 +75,8 @@ typedef struct remote_ctx {
 typedef struct remote {
     int fd;
     buffer_t *buf;
-    struct remote_ctx *recv_ctx;
-    struct remote_ctx *send_ctx;
+    remote_ctx_t *recv_ctx;
+    remote_ctx_t *send_ctx;
     struct server *server;
     uint32_t counter;
     struct sockaddr *addr;
diff --git a/src/udprelay.c b/src/udprelay.c
index 4c05889..7aa4d70 100644
--- a/src/udprelay.c
+++ b/src/udprelay.c
@@ -78,17 +78,6 @@ static server_ctx_t *server_ctx_list[MAX_REMOTE_NUM] = { NULL };
 
 const char *s_port = NULL;
 
-/* Non-blocking helper */
-static int
-setnonblocking(int fd)
-{
-    int flags;
-    if (-1 == (flags = fcntl(fd, F_GETFL, 0))) {
-        flags = 0;
-    }
-    return fcntl(fd, F_SETFL, flags | O_NONBLOCK);
-}
-
 #ifdef SO_NOSIGPIPE
 static int
 set_nosigpipe(int socket_fd)
@@ -134,37 +123,6 @@ hash_key_fill(char *out_key, const int af, const struct sockaddr_storage *addr)
     memcpy(out_key + sizeof(int), (const uint8_t *)addr, sizeof(struct sockaddr_storage));
 }
 
-static int
-construct_udprelay_header(const struct sockaddr_storage *in_addr,
-                          char *addr_header)
-{
-    int addr_header_len = 0;
-
-    if (in_addr->ss_family == AF_INET) {
-        struct sockaddr_in *addr = (struct sockaddr_in *)in_addr;
-        size_t addr_len          = sizeof(struct in_addr);
-
-        addr_header[addr_header_len++] = 1;
-        memcpy(addr_header + addr_header_len, &addr->sin_addr, addr_len);
-        addr_header_len += addr_len;
-        memcpy(addr_header + addr_header_len, &addr->sin_port, 2);
-        addr_header_len += 2;
-    } else if (in_addr->ss_family == AF_INET6) {
-        struct sockaddr_in6 *addr = (struct sockaddr_in6 *)in_addr;
-        size_t addr_len           = sizeof(struct in6_addr);
-
-        addr_header[addr_header_len++] = 4;
-        memcpy(addr_header + addr_header_len, &addr->sin6_addr, addr_len);
-        addr_header_len += addr_len;
-        memcpy(addr_header + addr_header_len, &addr->sin6_port, 2);
-        addr_header_len += 2;
-    } else {
-        return 0;
-    }
-
-    return addr_header_len;
-}
-
 static int
 parse_udprelay_header(const char *buf, const size_t buf_len,
                       char *host, char *port, struct sockaddr_storage *storage)
@@ -456,11 +414,11 @@ create_server_socket(const char *host, const char *port)
         int tos   = 46 << 2;
         int rc = setsockopt(server_sock, IPPROTO_IP, IP_TOS, &tos, sizeof(tos));
         if (rc < 0 && errno != ENOPROTOOPT) {
-            LOGE("setting ipv4 dscp failed: %d", errno);
+            ERROR("setting ipv4 dscp failed");
         }
         rc = setsockopt(server_sock, IPPROTO_IPV6, IPV6_TCLASS, &tos, sizeof(tos));
         if (rc < 0 && errno != ENOPROTOOPT) {
-            LOGE("setting ipv6 dscp failed: %d", errno);
+            ERROR("setting ipv6 dscp failed");
         }
 #endif
 
@@ -806,7 +764,7 @@ server_recv_cb(EV_P_ ev_io *w, int revents)
         set_nosigpipe(remotefd);
 #endif
 #ifdef SET_INTERFACE
-        if (server_ctx->iface) {
+        if (server_ctx->iface && strlen(server_ctx->iface) > 0) {
             if (setinterface(remotefd, server_ctx->iface) == -1) ERROR("setinterface");
         }
 #endif
@@ -862,9 +820,9 @@ server_recv_cb(EV_P_ ev_io *w, int revents)
     int s = sendto(remote_ctx->fd, buf->data, buf->len, 0, remote_addr, remote_addr_len);
     if (remote_ctx) remote_ctx->state = STATE_AWAITING_REPLY;
     if (s == -1) {
-        if (!(errno == EAGAIN || errno == EWOULDBLOCK)) {
+        if (errno != EAGAIN && errno != EWOULDBLOCK) {
             ERROR("[udp] server->sendto to remote failed");
-            /* If send fails immediately, mark this remote as down and try to find another one for next time */
+            /* If send fails immediately, mark remote as down and destroy this session */
             if (server_ctx->remote_num > 1) {
                 server_ctx->remote_status[remote_ctx->remote_idx] = false;
                 LOGI("[udp] sendto failed, marking remote index %d as offline", remote_ctx->remote_idx);
@@ -900,8 +858,9 @@ free_cb(void *key, void *element)
 /* 初始化 udprelay，支持多个 remote_addr，记录 preferred_remote_idx 并注册 failback timer */
 int
 init_udprelay(const char *server_host, const char *server_port, int remote_num,
-              struct sockaddr **remote_addr,
-              int mtu, crypto_t *crypto, int timeout, const char *iface, int fwmark)
+              struct sockaddr **remote_addr, int mtu, crypto_t *crypto,
+              int timeout, const char *iface, int fwmark,
+              volatile bool *remote_status)
 {
     s_port = server_port;
     struct ev_loop *loop = EV_DEFAULT;
@@ -931,10 +890,7 @@ init_udprelay(const char *server_host, const char *server_port, int remote_num,
     server_ctx->fwmark = fwmark;
     server_ctx->remote_num = remote_num;
     server_ctx->remote_addr = remote_addr;
-    server_ctx->remote_status = ss_malloc(sizeof(bool) * remote_num);
-    for (int i = 0; i < remote_num; i++) {
-        server_ctx->remote_status[i] = true; /* Assume all are up initially */
-    }
+    server_ctx->remote_status = remote_status;
 
     ev_io_start(loop, &server_ctx->io);
 
@@ -954,7 +910,9 @@ free_udprelay()
     while (server_num > 0) {
         server_ctx_t *server_ctx = server_ctx_list[--server_num];
         ev_io_stop(loop, &server_ctx->io);
-        ss_free((void *)server_ctx->remote_status);
+        /* Use a temporary variable to hold the pointer for ss_free */
+        void *status_ptr = (void *)server_ctx->remote_status;
+        ss_free(status_ptr);
         close(server_ctx->fd);
         cache_delete(server_ctx->conn_cache, 0);
         ss_free(server_ctx);
diff --git a/src/udprelay.h b/src/udprelay.h
index ab3d563..5229abf 100644
--- a/src/udprelay.h
+++ b/src/udprelay.h
@@ -53,8 +53,8 @@ typedef struct server_ctx {
     struct cache *conn_cache;
     int remote_num;
     struct sockaddr **remote_addr;
-    int preferred_remote_idx;
     int fwmark;
+    volatile bool *remote_status;
 } server_ctx_t;
 
 typedef enum {
@@ -69,7 +69,7 @@ typedef struct remote_ctx {
     int fd;
     struct sockaddr_storage src_addr;
     remote_state_t state;
-    struct server_ctx *server_ctx;
+    server_ctx_t *server_ctx;
     int remote_idx;
 } remote_ctx_t;
 
diff --git a/src/utils.c b/src/utils.c
index c1f6342..fe05f81 100644
--- a/src/utils.c
+++ b/src/utils.c
@@ -65,6 +65,16 @@ ERROR(const char *s)
 
 int use_tty = 1;
 
+int
+setnonblocking(int fd)
+{
+    int flags;
+    if (-1 == (flags = fcntl(fd, F_GETFL, 0))) {
+        flags = 0;
+    }
+    return fcntl(fd, F_SETFL, flags | O_NONBLOCK);
+}
+
 char *
 ss_itoa(int i)
 {
diff --git a/src/utils.h b/src/utils.h
index 450e3ec..4863f58 100644
--- a/src/utils.h
+++ b/src/utils.h
@@ -222,6 +222,7 @@ int run_as(const char *user);
 void FATAL(const char *msg);
 void usage(void);
 void daemonize(const char *path);
+int setnonblocking(int fd);
 char *ss_strndup(const char *s, size_t n);
 #ifdef HAVE_SETRLIMIT
 int set_nofile(int nofile);
-- 
2.37.1 (Apple Git-137.1)

